node_modules
.next
.env.local
.env*.local
.vercel
*.log
.DS_Store
tsconfig.tsbuildinfo

# Skills instaladas localmente via `npx skills add` (config de herramientas de
# desarrollo, no del producto) - cada dev las reinstala si las necesita.
.agents/
.claude/skills/
skills-lock.json
